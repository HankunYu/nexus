namespace Nexus.Networking.Core
{
    /// <summary>
    /// Determines whether the session operates over LAN or internet.
    /// </summary>
    public enum NexusMode
    {
        Local,
        Remote
    }
}
