namespace Nexus.Networking.Core
{
    /// <summary>
    /// The role this instance plays in the network session.
    /// </summary>
    public enum NetworkMode
    {
        None,
        Host,
        Client,
        Server
    }
}
